import React from "react";

import { StyledTableCell } from "../StyledTable";

export const ExchangeCol = ({ exchange }) => {
  return <StyledTableCell align="center">{exchange}</StyledTableCell>;
};
